#include <stdio.h>
/**
 * main - A function that executes a line using printf function
 * Return: 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,")
		return (0);
}
